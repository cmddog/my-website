name: Deploy frontend to Oracle

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 22

      - name: Install dependencies and build app
        working-directory: frontend
        run: |
          npm ci
          npm run build -- --configuration production

      - name: Copy files to Oracle via SCP
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: |
            Dockerfile
            Caddyfile
            dist/**
          target: "~/website-deploy"

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ~/website-deploy
            docker stop website || true
            docker rm website || true
            docker image prune -f
            docker build -t website -f frontend/Dockerfile .
            docker run -d --name website --restart unless-stopped -p 80:80 -p 443:443 website
